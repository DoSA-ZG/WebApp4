@model VrstaRada

@{
    ViewBag.Title = "Ažuriraj vrstu rada";
}

<h2>Ažuriraj vrstu rada</h2>

<form asp-route-page="@ViewBag.Page"
      asp-route-sort="@ViewBag.Sort"
      asp-route-ascending="@ViewBag.Ascending" method="post">
    <div asp-validation-summary="All"></div>
    <div class="mb-3">
        <label asp-for="VrstaRada1" class="form-label">Vrsta rada</label>
        <input asp-for="VrstaRada1" class="form-control" />
        <div class="form-text"><span asp-validation-for="VrstaRada1" class="text-danger"></span></div>
    </div>
    <div class="text-center">
        <button class="btn btn-primary" type="submit">Ažuriraj</button>
        <a asp-action="Index"
           asp-route-page="@ViewBag.Page"
           asp-route-sort="@ViewBag.Sort"
           asp-route-ascending="@ViewBag.Ascending"
           class="btn btn-secondary">Odustani</a>
    </div>
</form>
@section scripts {
    <partial name="IncludeValidation" />
}
