@model RPPP_WebApp.ViewModels.VrstaUlogeViewModel

<tr hx-target="this" hx-swap="outerHTML">
    <td class="text-left">@Model.Vrsta</td>
    <td>
        <a asp-action="Edit"
           asp-route-id="@Model.Id"
           asp-route-page="@ViewBag.PagingInfo.CurrentPage"
           asp-route-sort="@ViewBag.PagingInfo.Sort"
           asp-route-ascending="@ViewBag.PagingInfo.Ascending"
           class="btn btn-secondary btn-sm" title="Ažuriraj"><i class="fas fa-edit"></i></a>
    </td>
    <td>
        <button class="btn btn-sm btn-danger"
                hx-confirm="Obrisati vrstu uloge?"
                hx-delete="@Url.Action(nameof(VrstaUlogeController.Delete), new { id = Model.Id })"
                title="Obriši">
            <i class="fas fa-trash-alt"></i>
        </button>
    </td>
</tr>